[project]
name = "pydantic-ai-rlm"
version = "0.2.0"
description = "Monty-sandboxed Recursive Language Model toolset for Pydantic AI"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{name = "Pydantic AI RLM Contributors"}]
keywords = [
    "pydantic",
    "pydantic-ai",
    "llm",
    "ai",
    "machine-learning",
    "recursive",
    "context",
    "repl",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
dependencies = [
    "pydantic-ai-slim[cli]>=2.43.0,<2.44.0",
    "pydantic-monty>=0.0.22,<0.0.23",
]

[project.optional-dependencies]
dev = [
    "bandit>=1.9.4,<2",
    "mypy>=1.0,<2",
    "pip-audit>=2.10.1,<3",
    "psutil>=7.0,<8",
    "pytest>=9.0,<10",
    "pytest-asyncio>=1.0,<2",
    "pytest-timeout>=2.4,<3",
    "ruff>=0.14,<1",
]
logging = ["rich>=13.0"]
openrouter = [
    "pydantic-ai-slim[openai]>=2.43.0,<2.44.0",
    "python-dotenv>=1.1,<2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pydantic_ai_rlm"]

[dependency-groups]
dev = [
    "bandit>=1.9.4,<2",
    "mypy>=1.0,<2",
    "pip-audit>=2.10.1,<3",
    "pre-commit>=4.0.0",
    "psutil>=7.0,<8",
    "pytest>=9.0,<10",
    "pytest-asyncio>=1.0,<2",
    "pytest-timeout>=2.4,<3",
    "ruff>=0.14,<1",
    "ty>=0.0.10",
]


# Ruff configuration
[tool.ruff]
line-length = 135
target-version = "py310"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C",   # flake8-comprehensions
    "UP",  # pyupgrade (modern Python syntax)
    "SIM", # flake8-simplify
    "RUF", # Ruff-specific rules
    "N",   # pep8-naming
    "YTT", # flake8-2020
    "A",   # flake8-builtins
]

[tool.ruff.lint.per-file-ignores]
"src/pydantic_ai_rlm/prompts.py" = ["E501"]

[tool.ruff.lint.isort]
known-first-party = [
    "my_project",
] # Example: if your code is in src/my_projectme/
combine-as-imports = true

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "lf"
docstring-code-format = true

[tool.ty.rules]
unresolved-import = "ignore"
possibly-missing-attribute = "ignore"
